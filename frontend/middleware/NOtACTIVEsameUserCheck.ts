// export default defineNuxtRouteMiddleware((to, username) => {
//     if (process.client) {
//         const token = localStorage.getItem("token");
        
//         if (token != null) {
//             if (username ===)
//         }
        
//     }
// })
